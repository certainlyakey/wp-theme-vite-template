{% if post %}
  <li>

    {% include 'partials/thumbnail.twig' with { 'post': post, 'size': 'avatar', 'classes': 'thumbnail-class' } %}

    <a href="{{ post.link }}">
      <h3>{{ post.title }}</h3>
      <div>{{ post.excerpt }}</div>
    </a>

  </li>
{% endif %}
